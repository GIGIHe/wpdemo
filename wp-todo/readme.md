第一步首先安装依赖 npm init 不是npm i  
第二步 各种loader的下载和使用 webpack vue  vue-template-compiler webpack-cli css-loader style-loader vue-loader html-webpack-plugin url-loader
webpack-dev-server的安装与配置  
cross-env 跨平台的配置和使用环境变量
vue2简介
数据绑定 render方法  
>```
render: function (createElement) {
    return createElement(App);
}
```
postcss-loader